#cluster
#baseUrl=http://139.124.148.57:8080

#localhost
baseUrl=http://127.0.0.1:8080

#settings which allow you to define the maximum period of time which we should wait for a burst to complete some operation.
#The time is specified in <code>seconds</code>.
maxTimeToWaitForBurstToStart=600
maxTimeToWaitForBurstToFinish=3600

#with this value is called Thread.sleep().
#The time is specified in <code>seconds</code>.
timeToWaitForBurstToLoad=5


#how much we should wait for an element to appear on the page before make the test to fail
#if the element is on the page the test won't wait
#The time is specified in <code>seconds</code>.
timeout=30
