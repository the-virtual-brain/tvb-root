#contains the custom properties used into the jmx reports

#Properties are not the same as variables. Variables are local to a thread; properties
#are common to all threads, and need to be referenced using the __P or __property function.

#server and port against which should be run the jmeter tests
server.host=localhost
server.port=8080