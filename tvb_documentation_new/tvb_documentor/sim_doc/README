To generate the simulator reference docs:

  first generate the class diagrams:
  
    sh class_diagrams.sh
  
  and the phase-planes (you need TVB path stuff set), first start ipython:
  
    ipython
  
  then, at the ipython prompt, run the generator script
  
    run generate_model_phase_plane_images.py
  
  
  now use Sphinx via the Makefile to build the web pages
  
    make clean
    make html

