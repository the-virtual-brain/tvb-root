rem Runs the web part of the software and the computation back-end server, both on your local machine.
python app.py start web 

rem Runs the desktop client interface of TVB, on the local machine.
rem export VERSIONER_PYTHON_PREFER_32_BIT=yes
rem start python app.py start desktop 

rem Starts a IDLE console with the correct environment set
rem python app.py console
