Attention!
Before exporting each file to kubernetes, in order to select the namespace, usernames and passwords, you have to manually change
the keywords ${NAMESPACE}, ${USER}, ${PASSWORD}, ${ADMIN_NAME} , ${ADMIN_PASSWORD}.